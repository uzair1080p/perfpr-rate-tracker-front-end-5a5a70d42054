import React, { useEffect } from 'react';
import { View } from 'react-native';

export default function SelectTransactions({setTransactions, transactions}) {
  useEffect(() => {

  }, []);

  return (
    <View>
    </View>
  );
};

const styles = {};